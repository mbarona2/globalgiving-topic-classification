from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project uses the GlobalGiving API to collect projects and classify them under a theme as an auto-tag application',
    author='Melissa Barona',
    license='MIT',
)
